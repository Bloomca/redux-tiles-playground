{
  "name": "redux-tiles-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "rm -rf dist/.git && npm run build && cd dist && git init && git add . && git commit -m \"Initial commit\" && git remote add origin git@github.com:Bloomca/redux-tiles-playground.git && git push --force origin master:gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bloomca/redux-tiles-playground.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Bloomca/redux-tiles-playground/issues"
  },
  "homepage": "https://github.com/Bloomca/redux-tiles-playground#readme",
  "devDependencies": {
    "parcel-bundler": "^1.2.0"
  }
}
